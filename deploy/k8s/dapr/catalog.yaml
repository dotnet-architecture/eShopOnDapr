kind: ConfigMap
apiVersion: v1
metadata:
  name: catalog-cm
  namespace: eshop
  labels:
    app: eshop
    service: catalog
data:
  ASPNETCORE_ENVIRONMENT: Development
  AzureServiceBusEnabled: "False"
  AzureStorageEnabled: "False"
  ConnectionString: Server=sqldata;Database=Microsoft.eShopOnContainers.Services.CatalogDb;User Id=sa;Password=Pass@word
  EventBusConnection: rabbitmq
  OrchestratorType: K8S
  PATH_BASE: /catalog-api
  PicBaseUrl: http://apigateway/c/api/v1/catalog/items/[0]/pic/
  PORT: "80"
  UseCustomizationData: "False"

---

kind: Deployment
apiVersion: apps/v1
metadata:
  name: catalog
  namespace: eshop
  labels:
    app: eshop
    service: catalog
spec:
  replicas: 1
  selector:
    matchLabels:
      service: catalog
  template:
    metadata:
      labels:
        app: eshop
        service: catalog
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "catalog-api"
        dapr.io/app-port: "80"
    spec:
      containers:
        - name: catalog-api
          image: eshop/catalog.api:linux-latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
              protocol: TCP
            - containerPort: 81
              protocol: TCP
          envFrom:
            - configMapRef:
                name: catalog-cm

---

kind: Service
apiVersion: v1
metadata:
  name: catalog-api
  namespace: eshop
  labels:
    app: eshop
    service: catalog
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30003
      protocol: TCP
      name: http
  selector:
    service: catalog
